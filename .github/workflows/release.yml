name: Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  id-token: write
  pull-requests: write

jobs:
  release:
    runs-on: ubuntu-latest
    env:
      RELEASE_GITHUB_TOKEN: ${{ secrets.RELEASE_PR_TOKEN || secrets.GITHUB_TOKEN }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          token: ${{ env.RELEASE_GITHUB_TOKEN }}

      - name: Set up pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10.18.3

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: pnpm
          registry-url: https://registry.npmjs.org/

      - name: Initialize Codex latest ref
        run: |
          latest_ref="@call-e/codex-plugin@latest"

          if git ls-remote --exit-code --tags origin "$latest_ref" >/dev/null 2>&1; then
            echo "${latest_ref} already exists."
            exit 0
          fi

          codex_version=$(node --input-type=module <<'EOF'
          import fs from "node:fs";

          const packageJson = JSON.parse(fs.readFileSync("packages/codex-plugin/package.json", "utf8"));
          console.log(packageJson.version);
          EOF
          )
          release_ref="@call-e/codex-plugin@${codex_version}"

          if ! release_commit=$(git rev-parse --verify --quiet "${release_ref}^{}"); then
            echo "No local ${release_ref} tag found; leaving ${latest_ref} uninitialized."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag "$latest_ref" "$release_commit"
          git push origin "refs/tags/${latest_ref}"

      - name: Initialize Claude latest ref
        run: |
          latest_ref="@call-e/claude-plugin@latest"

          if git ls-remote --exit-code --tags origin "$latest_ref" >/dev/null 2>&1; then
            echo "${latest_ref} already exists."
            exit 0
          fi

          claude_version=$(node --input-type=module <<'EOF'
          import fs from "node:fs";

          const packageJson = JSON.parse(fs.readFileSync("packages/claude-plugin/package.json", "utf8"));
          console.log(packageJson.version);
          EOF
          )
          release_ref="@call-e/claude-plugin@${claude_version}"

          if ! release_commit=$(git rev-parse --verify --quiet "${release_ref}^{}"); then
            echo "No local ${release_ref} tag found; leaving ${latest_ref} uninitialized."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag "$latest_ref" "$release_commit"
          git push origin "refs/tags/${latest_ref}"

      - name: Initialize Cursor latest ref
        run: |
          latest_ref="@call-e/cursor-plugin@latest"

          if git ls-remote --exit-code --tags origin "$latest_ref" >/dev/null 2>&1; then
            echo "${latest_ref} already exists."
            exit 0
          fi

          cursor_version=$(node --input-type=module <<'EOF'
          import fs from "node:fs";

          const packageJson = JSON.parse(fs.readFileSync("packages/cursor-plugin/package.json", "utf8"));
          console.log(packageJson.version);
          EOF
          )
          release_ref="@call-e/cursor-plugin@${cursor_version}"

          if ! release_commit=$(git rev-parse --verify --quiet "${release_ref}^{}"); then
            echo "No local ${release_ref} tag found; leaving ${latest_ref} uninitialized."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag "$latest_ref" "$release_commit"
          git push origin "refs/tags/${latest_ref}"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Create release PR or publish
        id: changesets
        uses: changesets/action@v1
        with:
          version: pnpm run version-packages
          publish: pnpm run publish-packages
          commit: "chore: release packages"
          title: "chore: release packages"
        env:
          GITHUB_TOKEN: ${{ env.RELEASE_GITHUB_TOKEN }}

      - name: Update Codex latest ref
        if: steps.changesets.outputs.published == 'true'
        env:
          PUBLISHED_PACKAGES: ${{ steps.changesets.outputs.publishedPackages }}
        run: |
          latest_ref="@call-e/codex-plugin@latest"

          should_update=$(node --input-type=module <<'EOF'
          const packages = JSON.parse(process.env.PUBLISHED_PACKAGES || "[]");
          const codexPlugin = packages.find((pkg) => pkg.name === "@call-e/codex-plugin");

          if (!codexPlugin) {
            console.error("No @call-e/codex-plugin package was published; leaving @call-e/codex-plugin@latest unchanged.");
            console.log("false");
            process.exit(0);
          }

          console.error(`Updating @call-e/codex-plugin@latest for @call-e/codex-plugin@${codexPlugin.version}.`);
          console.log("true");
          EOF
          )

          if [ "$should_update" != "true" ]; then
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -f "$latest_ref" "$GITHUB_SHA"
          git push origin "refs/tags/${latest_ref}" --force

      - name: Update Claude latest ref
        if: steps.changesets.outputs.published == 'true'
        env:
          PUBLISHED_PACKAGES: ${{ steps.changesets.outputs.publishedPackages }}
        run: |
          latest_ref="@call-e/claude-plugin@latest"

          should_update=$(node --input-type=module <<'EOF'
          const packages = JSON.parse(process.env.PUBLISHED_PACKAGES || "[]");
          const claudePlugin = packages.find((pkg) => pkg.name === "@call-e/claude-plugin");

          if (!claudePlugin) {
            console.error("No @call-e/claude-plugin package was published; leaving @call-e/claude-plugin@latest unchanged.");
            console.log("false");
            process.exit(0);
          }

          console.error(`Updating @call-e/claude-plugin@latest for @call-e/claude-plugin@${claudePlugin.version}.`);
          console.log("true");
          EOF
          )

          if [ "$should_update" != "true" ]; then
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -f "$latest_ref" "$GITHUB_SHA"
          git push origin "refs/tags/${latest_ref}" --force

      - name: Update Cursor latest ref
        if: steps.changesets.outputs.published == 'true'
        env:
          PUBLISHED_PACKAGES: ${{ steps.changesets.outputs.publishedPackages }}
        run: |
          latest_ref="@call-e/cursor-plugin@latest"

          should_update=$(node --input-type=module <<'EOF'
          const packages = JSON.parse(process.env.PUBLISHED_PACKAGES || "[]");
          const cursorPlugin = packages.find((pkg) => pkg.name === "@call-e/cursor-plugin");

          if (!cursorPlugin) {
            console.error("No @call-e/cursor-plugin package was published; leaving @call-e/cursor-plugin@latest unchanged.");
            console.log("false");
            process.exit(0);
          }

          console.error(`Updating @call-e/cursor-plugin@latest for @call-e/cursor-plugin@${cursorPlugin.version}.`);
          console.log("true");
          EOF
          )

          if [ "$should_update" != "true" ]; then
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -f "$latest_ref" "$GITHUB_SHA"
          git push origin "refs/tags/${latest_ref}" --force
