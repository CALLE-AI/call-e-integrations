---
"@call-e/claude-plugin": minor
---

Switch the Claude Code plugin to the shared `calle` CLI flow so authentication can be checked and recovered on use instead of requiring manual remote MCP authorization.
