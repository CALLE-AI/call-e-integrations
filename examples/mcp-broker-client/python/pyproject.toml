[project]
name = "calle-mcp-broker-client-example-python"
version = "0.0.0"
description = "Minimal CALL-E broker login MCP client example."
requires-python = ">=3.10"
dependencies = [
  "httpx>=0.28.1",
]

[dependency-groups]
dev = [
  "pytest>=9.0.0",
]
